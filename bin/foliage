#!/usr/bin/env ruby
# -*- ruby -*-

require 'foliage'
at_exit do
  puts Foliage.report(Foliage::BranchTable.pop)
end
Foliage::BranchTable.push([])
Foliage.instr_eval(File.read(ARGV[0]), binding, ARGV[0])
